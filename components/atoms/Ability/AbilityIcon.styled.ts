// Modules
import styled from "styled-components";

// Components
import { SvgIcon as MaterialSvgIcon } from "@material-ui/core";

// Styles
import { primary } from "styles/customColors";

// Elements
export const SvgIcon = styled(MaterialSvgIcon)`
	color: ${primary};
	height: 100%;
	width: 100%;
`;
